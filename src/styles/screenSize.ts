export const phone = `max-width: 25em`

export const isPhoneOrTable = () => window.innerWidth <= 600

export const isPhone = () => window.innerWidth <= 400

export const isTablet=()=> window.innerWidth <= 600 && window.innerWidth > 400

export const isDesktop = () => window.innerWidth > 600
